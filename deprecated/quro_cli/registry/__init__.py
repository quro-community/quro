"""
PostgreSQL registry for symbol metadata and morphisms

Provides CRUD operations for:
- File morphisms (LSH signatures, exports)
- Symbol metadata (role, intent, tags)
- Dependencies (cross-file references)
- Snapshots (git-anchored workspace state)
"""
